{
  "name": "gaon-sathi-server",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny local proxy so the Gemini API key never sits in frontend code.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
